displayName=Color Locked
build=standard
author=unidarkshin
description=Group Iron color-lock for the Color Lock hub (group.thegrandchart.com). Sync your team color and item rules; block wrong-color eat/equip/wield and restricted pickaxes/axes; mark restricted items in inv/bank/gear; lookup sidebar. Quest gear uses normal color rules—while that quest is in progress, matching tools are temporarily allowed for any team color. Manual color mode and offline rules from a local cache when sync is off or the hub is unreachable.
plugins=com.osrscolorlock.colorlock.ColorLockPlugin
tags=ironman,gim,groupiron,color-lock,color,lock,restriction,quest,hub
